@using Home.Recipes.WebClient.Components.Buttons

<p>Are you sure you want to delete this?</p>

<div class="bg-gray-50 flex flex-row-reverse mt-2">
    <DeleteButton @onclick="@(async () => await SubmitForm())">Delete</DeleteButton>
    <SecondaryButton class="mx-1" @onclick="@(async () => await Cancel())">Cancel</SecondaryButton>
</div>

@code {
    [CascadingParameter] BlazoredModalInstance BlazoredModal { get; set; } = default!;

    private async Task SubmitForm() => await BlazoredModal.CloseAsync(ModalResult.Ok(true));
    private async Task Cancel() => await BlazoredModal.CancelAsync();
}
